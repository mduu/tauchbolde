@model Tauchbolde.InterfaceAdapters.Event.List.EventListViewModel
@{
    ViewBag.Title = "Aktivitäten";
    Layout = "_Layout";
}

@* HACK FOR FIREFOX AND RESPONSIVE TABLES *@
<style>
@@-moz-document url-prefix() {
    fieldset {
        display: table-cell;
    }
}
</style>

<h2>Aktivitäten</h2>

<div class="table-responsive">
    <table class="table table-hover">
        <tr>
            <th></th>
            <th>@Html.DisplayNameFor(m => m.Rows.First().StartEndTime)</th>
            <th>@Html.DisplayNameFor(m => m.Rows.First().Title)</th>
            <th>@Html.DisplayNameFor(m => m.Rows.First().Location)</th>
            <th>@Html.DisplayNameFor(m => m.Rows.First().MeetingPoint)</th>
        </tr>
        @foreach (var upcommingEvent in Model.Rows)
        {
            <tr>
                <td>
                    <a class="btn btn-primary btn-sm" asp-action="Details" asp-route-id="@upcommingEvent.EventId"><span class="glyphicon glyphicon-search"></span></a>
                </td>
                <td>@upcommingEvent.StartEndTime</td>
                <td>@upcommingEvent.Title</td>
                <td>@upcommingEvent.Location</td>
                <td>@upcommingEvent.MeetingPoint</td>
            </tr>
        }
    </table>
</div>
<a href="@Url.Action("Edit")" id="create-event" class="btn btn-primary"><strong>Neue Aktivität</strong></a>