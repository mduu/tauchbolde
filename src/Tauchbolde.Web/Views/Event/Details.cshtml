@using Tauchbolde.Common.Model
@model Tauchbolde.Common.Model.Event
@{
    ViewBag.Title = $"Aktivität - {Model.Name}";
    Layout = "_Layout";
}

<div class="container">
    <div class="row">
        <h2>Aktivität - @Model.Name</h2>
    </div>

    <div class="row">

        <div class="col-md-6 container">
            <div class="row">
                <label class="col-md-6" asp-for="Name"></label>
                <span lass>@Model.Name</span>
            </div>
            <div class="row">
                <label class="col-md-6" asp-for="Location"></label>
                <span lass>@Model.Location</span>
            </div>
            <div class="row">
                <label class="col-md-6" asp-for="MeetingPoint"></label>
                <span lass>@Model.MeetingPoint</span>
            </div>
            <div class="row">
                <label class="col-md-6" asp-for="Organisator"></label>
                <span lass>@Model.Organisator</span>
            </div>
            <div class="row">
                <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-default">Bearbeiten</a>
            </div>
        </div>

        <div class="col-md-6">
            <div class="container">
                <h2>Teilnehmer</h2>

                <h3>Angemeldet:</h3>
                @foreach (var participant in Model.Participants.Where(p => p.Status == ParticipantStatus.Accepted))
                {
                    <div class="row">
                        <div class="col-sm-1"><span class="glyphicon glyphicon-thumbs-up" aria-hidden="true"></span></div>
                        <div class="col-sm-1">(@participant.CountPeople)</div>
                        <div class="col-sm-10">@participant.User.UserName <em>@participant.</em></div>
                    </div>
                }

                <h3>Unsicher:</h3>
                @foreach (var participant in Model.Participants.Where(p => p.Status == ParticipantStatus.Tentative || p.Status == ParticipantStatus.None))
                {
                    <div class="row">
                        <div class="col-sm-1"><span class="glyphicon glyphicon-hourglass" aria-hidden="true"></span></div>
                        <div class="col-sm-1">(@participant.CountPeople)</div>
                        <div class="col-sm-10">@participant.User.UserName <em>@participant.</em></div>
                    </div>
                }

                <h3>Abgmeldet:</h3>
                @foreach (var participant in Model.Participants.Where(p => p.Status == ParticipantStatus.Declined))
                {
                    <div class="row">
                        <div class="col-sm-1"><span class="glyphicon glyphicon-thumbs-down" aria-hidden="true"></span></div>
                        <div class="col-sm-1">(@participant.CountPeople)</div>
                        <div class="col-sm-10">@participant.User.UserName <em>@participant.</em></div>
                    </div>
                }


                <a class="btn btn-primary" role="button" data-toggle="collapse" href="#collapseExample" aria-expanded="false" aria-controls="collapseExample">
                    Link with href
                </a>

                <div class="collapse" id="collapseExample">

                    <div class="btn-group" data-toggle="buttons">
                        <label class="btn btn-success active">
                            <input type="radio" name="options" id="option1" autocomplete="off" checked> Bin dabei
                        </label>
                        <label class="btn btn-default">
                            <input type="radio" name="options" id="option2" autocomplete="off"> Weiss noch nicht
                        </label>
                        <label class="btn btn-danger">
                            <input type="radio" name="options" id="option3" autocomplete="off"> Kann leider nicht
                        </label>
                    </div>

                </div>
            </div>
        </div>

    </div>
</div>